[
  {
    "layer": "top", // Waybar on highest layer so tooltips go over windows
    "output": "eDP-1", // Set output to primary monitor
    "height": 36, // Set height to avoid jumping due to active workspace indicator

    "modules-left": ["hyprland/workspaces", "custom/spotify", "hyprland/window"], // Sets modules for the left of the bar
    "modules-center": ["clock"], // Set modules for the center of the bar
    "modules-right": ["tray", "cpu", "memory", "temperature", "battery", "backlight", "pulseaudio", "network","custom/notification", "custom/exit"], // Set modules for the right of the bar

    "include": ["~/.config/waybar/modules.jsonc"] // Include the module config
  },

  {
    "layer": "top", // Waybar on highest layer so tooltips go over windows
    "output": "DP-1", // Set output to secondary monitor
    "height": 36, // Set height to avoid jumping due to active workspace indicator

    "modules-left": ["hyprland/workspaces", "clock", "custom/kernel", "mpd"], // Sets modules for the left of the bar
    "modules-center": ["hyprland/window"], // Sets modules for the center of the bar
    "modules-right": ["tray", "temperature", "cpu", "memory", "pulseaudio", "network"], // Sets modules for the right of the bar

    "include": ["~/.config/waybar/modules.jsonc"] // Include the module config
  }
]

